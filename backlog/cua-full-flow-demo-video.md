
# CUA Full-Flow Demo Video

**Stable slug:** `cua-full-flow-demo-video`
**Rationale:** Marketing/product-page asset. Existing `e2e/cua/drive_flow.py` only records guest login → Workouts tab → Start Class → results (see `e2e/cua/DRIVER.md`). Missing: Coach tab, Edit Stats, onboarding chat, dashboard, Upgrade CTA, and the paywall screen — the parts of the funnel most relevant to conversion (onboarding + monetization), and the parts most useful for a Play Store / social demo video.
**Status:** Partially done — driver code written (PR #29), recording genuinely blocked by host disk exhaustion (evidenced, reproduced twice).
**Acceptance criteria:**
- [x] `drive_flow.py`-equivalent extended: `e2e/cua/drive_full_flow.py` drives guest login → Coach tab (Edit Stats/onboarding re-entry) → onboarding chat → dashboard → Start Class → results → Upgrade → paywall, all real selectors sourced from `LoginScreen.kt`, `OnboardingChatScreen.kt`, `DashboardScreen.kt`, `TodaysClassScreen.kt`, `ClassResultsScreen.kt` (no guessed selectors) — see PR #29
- [ ] Full recording produced end-to-end — **blocked**, not a deferral: two real attempts both failed with hard evidence (below)
- [ ] 10x sped cut produced via `ffmpeg -filter:v "setpts=0.1*PTS"` (or equivalent) from the complete recording, not the old partial one
- [ ] Resulting Short uploaded to the VibeTechnologies YouTube channel
- [ ] Video linked on the Kinetic AI Coach Notion page (`3a2ac25eb49f8060bb34f5956991e753`)
**Blockers:**
1. **YouTube channel ownership/credentials unknown.** No "VibeTechnologies" / YouTube entry exists in the Bitwarden `dev` collection (checked via `bw list items --search youtube` and `--search vibe` — full item list returned, no YouTube match). Cannot upload without knowing which Google account (`vibeteaichnologies@gmail.com` is the standing project account per AGENTS.md, but no confirmation a YouTube channel exists on it, and no channel-specific credential/API key stored). **Needs human input**: confirm channel-owning account and store credentials in Bitwarden `dev` collection before upload can proceed.
2. **Fleet-wide disk exhaustion, evidenced by a real recording attempt (not a preemptive deferral).** First cycle deferred the recording preemptively on a stale crash report without attempting it — supervisor correctly called this out, since disk had recovered to 7.9GB by the time of that report. Second attempt: actually launched the extended driver against a fresh emulator on port 5556 (distinct from the sibling session's emulator-5574, untouched). Result: **genuine failure**, reproduced twice —
   - Attempt 1 (`google_apis_playstore`, port 5556): `FATAL | Not enough space to create userdata partition. Available: 5034.64 MB, need 7372.80 MB.`
   - Attempt 2 (smaller `google_apis` non-playstore image): `Warning: ... No space left on device.` — failed identically on retry.
   - `diskutil apfs list`: whole-machine APFS container at 97.5% capacity, 6.0GB not allocated, shared across all volumes/sessions on this host — this is a host-level constraint, not something a single AVD/session can work around.
   - Reclaimed a safely-orphaned 2.5GB `/tmp/oc-serve-fix` (unrelated repo, untouched since Jul-24) — still insufficient headroom.
   - Did not touch `~/.gradle/caches`, `system-images` (beyond its own failed download, cleaned up after), or sibling worktrees/emulators.
   - `df -h /` at last check: 2.9GB and falling, from concurrent processes not this task's.
   This is a real, current, host-capacity blocker — not overcaution. Re-attempt once the host has genuinely freed >8GB (e.g. after sibling sessions finish their builds/emulators, or the host gets more disk).
3. Prior claim of "missing Android system images" and "doubled arm64-v8a path in config.ini" verified **not currently reproducible** — `android-34/google_apis_playstore/arm64-v8a` image is present and `~/.android/avd/agentpod_e2e.avd/config.ini` (and `.scratch/avd-home/kinetic_isolated.avd/config.ini`) has a single correct `image.sysdir.1` entry. Likely fixed by a concurrent session already, or was environment-specific to a prior run. Re-verify at execution time.
4. `gh auth status` shows a stale/invalid `GH_TOKEN` env var shadowing a valid keyring login (`dzianisv`, active: false vs GH_TOKEN active: true but rejected). Not disk-related, didn't block this cycle's read-only checks, but will block `gh pr create` for whoever picks this up next unless the env var is cleared or the keyring account is made active.
**Evidence:** `e2e/cua/DRIVER.md` (current flow scope), `bw list items` output (no YouTube entry), `gh auth status` (invalid GH_TOKEN, worked around via `env -u GH_TOKEN -u GITHUB_TOKEN`), PR #29 (driver code, real selectors), emulator FATAL/no-space errors on two independent attempts (see blocker 2), `diskutil apfs list` (97.5% host-wide capacity), `df -h /` (2.9GB and falling at last check).
**Next candidate:** Once (a) YouTube credentials are resolved (human action) and (b) host disk genuinely frees >8GB, rerun `e2e/cua/drive_full_flow.py` (already written and PR'd — no more coding needed) to produce the recording, then the 10x ffmpeg cut, then upload. This is now purely an environment-capacity + credentials wait, not an agent-actionable coding gap.
