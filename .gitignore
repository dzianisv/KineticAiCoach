*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env
debug.keystore

# Release signing (upload keystore + credentials) — never commit these.
# Real secrets live in Bitwarden (the 'dev' collection); keystore.properties.template
# documents the expected format.
keystore.properties
*.keystore
*.jks

# Firebase provisioning (local secrets/handoff — do not commit)
.firebase-provision.local
app/google-services.json
.scratch/
functions/node_modules/
.agents/
firebase-debug.log

# Agent working artifacts (screenshots, scratch)
.agent-artifacts/
